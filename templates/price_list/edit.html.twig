{% extends 'base.html.twig' %}

{% block title %}Editar lista de precios{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Editar lista de precios</h1>
    <a class="btn btn-outline-dark" href="{{ path('app_price_list_products', {id: priceList.id}) }}">Gestionar precios</a>
</div>
{% include 'price_list/_form.html.twig' with {form: form} %}
{% endblock %}
