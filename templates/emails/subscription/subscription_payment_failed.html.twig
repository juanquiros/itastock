{% extends 'emails/_layout.html.twig' %}

{% block title %}Pago rechazado{% endblock %}

{% block content %}
    <h2 style="margin-top: 0;">Pago rechazado</h2>
    <p>Hola {{ userName|default('') }}, el pago de tu suscripción no pudo completarse.</p>
    <p>Plan: <strong>{{ planName|default('N/D') }}</strong></p>
    <p>Intento: <strong>{{ attemptedAt is defined and attemptedAt ? attemptedAt|date('d/m/Y') : 'N/D' }}</strong></p>
    <p>Motivo: <strong>{{ failureReason|default('N/D') }}</strong></p>
    <p style="margin: 24px 0;">
        <a href="{{ absolute_url(path('app_billing_subscription_show')) }}" style="background-color: #C62828; color: #ffffff; padding: 12px 18px; text-decoration: none; border-radius: 6px;">Actualizar medio de pago</a>
    </p>
{% endblock %}
