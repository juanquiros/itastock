{% extends 'emails/_layout.html.twig' %}

{% block title %}Nueva suscripción{% endblock %}

{% block content %}
    <h2 style="margin-top: 0;">Nueva suscripción activada</h2>
    <p>Se activó una nueva suscripción en la plataforma.</p>
    <p>Comercio: <strong>{{ businessName|default('N/D') }}</strong></p>
    <p>Admin: <strong>{{ adminEmail|default('N/D') }}</strong></p>
    <p>Plan: <strong>{{ planName|default('N/D') }}</strong></p>
    <p style="margin: 24px 0;">
        <a href="{{ absolute_url(path('platform_subscriptions_index')) }}" style="background-color: #C62828; color: #ffffff; padding: 12px 18px; text-decoration: none; border-radius: 6px;">Ver suscripciones</a>
    </p>
{% endblock %}
