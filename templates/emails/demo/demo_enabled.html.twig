{% extends 'emails/_layout.html.twig' %}

{% block title %}Demo habilitada{% endblock %}

{% block content %}
    <h2 style="margin-top: 0;">¡Tu demo está lista!</h2>
    <p>Hola {{ userName|default('') }}, ya podés ingresar a ItaStock y empezar a cargar tu información.</p>
    <p>Comercio: <strong>{{ businessName|default('N/D') }}</strong></p>
    <p>Fin de demo: <strong>{{ trialEndsAt is defined and trialEndsAt ? trialEndsAt|date('d/m/Y') : 'N/D' }}</strong></p>
    <p style="margin: 24px 0;">
        <a href="{{ absolute_url(path('app_login')) }}" style="background-color: #C62828; color: #ffffff; padding: 12px 18px; text-decoration: none; border-radius: 6px;">Ingresar a ItaStock</a>
    </p>
{% endblock %}
