{% extends 'base.html.twig' %}

{% block title %}Página no encontrada | ItaStock{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-body p-4 text-center">
                    <h1 class="h4 mb-3">Página no encontrada</h1>
                    <p class="text-muted mb-4">Lo que buscás no existe o fue movido.</p>
                    <a class="btn btn-primary" href="{{ app.user ? path('app_dashboard') : path('app_login') }}">Ir al inicio</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
