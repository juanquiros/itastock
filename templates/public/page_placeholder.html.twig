{% extends 'public/base_public.html.twig' %}

{% block title %}{{ title }} | ItaStock{% endblock %}

{% block body %}
    <section class="hero text-center">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card border-0 shadow-sm">
                    <div class="card-body p-5">
                        <div class="mb-3">
                            <span class="badge bg-warning text-dark">En construcción</span>
                        </div>
                        <h1 class="fw-bold mb-3">{{ title }}</h1>
                        <p class="lead text-muted mb-4">{{ message }}</p>
                        <a class="btn btn-primary" href="{{ path('public_home') }}">Volver al inicio</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
