{% extends 'public/base_public.html.twig' %}

{% block title %}Contacto | ItaStock{% endblock %}
{% block meta_description %}
    <meta name="description" content="Conecta con ItaStock para conocer cómo podemos ayudar a tu comercio con gestión de stock y ventas.">
{% endblock %}

{% block body %}
    <section class="hero">
        <div class="row align-items-center">
            <div class="col-lg-6 mb-4 mb-lg-0">
                <span class="badge bg-light text-dark border px-3 py-2 rounded-pill">Hablemos</span>
                <h1 class="fw-bold display-6 mt-3 mb-3">Cuéntanos sobre tu negocio</h1>
                <p class="lead text-muted">Déjanos tus datos y un especialista te contactará para ayudarte a implementar ItaStock.</p>
                <div class="d-flex flex-column gap-2 text-muted mt-4">
                    <div class="d-flex align-items-center gap-2">
                        <span class="badge bg-light text-dark border rounded-pill px-3 py-2">1</span>
                        <span>Completa el formulario</span>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                        <span class="badge bg-light text-dark border rounded-pill px-3 py-2">2</span>
                        <span>Nos contactamos para entender tus necesidades</span>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                        <span class="badge bg-light text-dark border rounded-pill px-3 py-2">3</span>
                        <span>Configuramos juntos tu cuenta</span>
                    </div>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="card border-0 shadow-sm">
                    <div class="card-body p-4 p-lg-5">
                        {{ form_start(contactForm) }}
                            <div class="mb-3">
                                {{ form_label(contactForm.name) }}
                                {{ form_widget(contactForm.name, {attr: {class: 'form-control'}}) }}
                                <div class="form-text text-danger">
                                    {{ form_errors(contactForm.name) }}
                                </div>
                            </div>
                            <div class="mb-3">
                                {{ form_label(contactForm.email) }}
                                {{ form_widget(contactForm.email, {attr: {class: 'form-control'}}) }}
                                <div class="form-text text-danger">
                                    {{ form_errors(contactForm.email) }}
                                </div>
                            </div>
                            <div class="mb-3">
                                {{ form_label(contactForm.phone) }}
                                {{ form_widget(contactForm.phone, {attr: {class: 'form-control'}}) }}
                                <div class="form-text text-danger">
                                    {{ form_errors(contactForm.phone) }}
                                </div>
                            </div>
                            <div class="mb-3">
                                {{ form_label(contactForm.message) }}
                                {{ form_widget(contactForm.message, {attr: {class: 'form-control'}}) }}
                                <div class="form-text text-danger">
                                    {{ form_errors(contactForm.message) }}
                                </div>
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary btn-lg">Enviar consulta</button>
                            </div>
                        {{ form_end(contactForm) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
