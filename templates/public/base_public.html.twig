<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}ItaStock{% endblock %}</title>
        {% block meta_description %}{% endblock %}
        {% block meta_image %}{% endblock %}
        <link rel="icon" type="image/svg+xml" href="{{ asset('brand/itastock-logo.svg') }}">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <style>
            :root {
                --ita-red: #C62828;
                --ita-dark: #1B1B1B;
                --ita-orange: #F57C00;
                --ita-gray: #F5F7FA;
            }

            body {
                background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
                font-family: 'Inter', system-ui, -apple-system, sans-serif;
                color: #1f1f1f;
            }

            .navbar {
                background-color: #ffffff;
                border-bottom: 1px solid #eceff4;
            }

            .navbar-brand {
                font-weight: 700;
                color: var(--ita-dark);
                letter-spacing: 0.01em;
            }

            .navbar-brand:hover {
                color: var(--ita-dark);
            }

            .nav-link {
                font-weight: 600;
                color: #3b4252;
            }

            .nav-link:hover,
            .nav-link:focus {
                color: var(--ita-red);
            }

            .btn-primary {
                background-color: var(--ita-red);
                border-color: var(--ita-red);
                font-weight: 600;
                box-shadow: 0 8px 16px rgba(198, 40, 40, 0.2);
            }

            .btn-primary:hover,
            .btn-primary:focus {
                background-color: #a91f1f;
                border-color: #a91f1f;
            }

            .btn-outline-dark {
                border-color: #d8dee9;
            }

            .hero {
                padding: 4rem 0 2rem 0;
            }

            .badge-featured {
                background: linear-gradient(90deg, var(--ita-red), var(--ita-orange));
            }

            .whatsapp-float {
                position: fixed;
                right: 24px;
                bottom: 24px;
                width: 56px;
                height: 56px;
                border-radius: 999px;
                background: #25d366;
                color: #ffffff;
                display: inline-flex;
                align-items: center;
                justify-content: center;
                box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
                z-index: 1050;
                transition: transform 0.2s ease, box-shadow 0.2s ease;
            }

            .whatsapp-float:hover,
            .whatsapp-float:focus {
                color: #ffffff;
                transform: translateY(-2px);
                box-shadow: 0 16px 28px rgba(0, 0, 0, 0.25);
            }

            footer {
                border-top: 1px solid #eceff4;
                background: #ffffff;
            }
        </style>
        {% block stylesheets %}{% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous" defer></script>
        {% block javascripts %}{% endblock %}
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-light">
                <div class="container">
                    <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('public_home') }}">
                        <img src="{{ asset('brand/itastock-logo.svg') }}" alt="ItaStock" width="36" height="36">
                        <span>ItaStock</span>
                    </a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Alternar navegación">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center gap-lg-2">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('public_home') }}">Home</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('public_features') }}">Funcionalidades</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('public_pricing') }}">Precios</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('public_contact') }}">Contacto</a>
                            </li>
                            <li class="nav-item ms-lg-2">
                                <a class="btn btn-primary" href="{{ path('app_login') }}">Ingresar</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </header>

        <main class="container">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show mt-3" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

        <footer class="py-4 mt-5">
            <div class="container d-flex flex-column flex-lg-row justify-content-between align-items-center gap-3">
                <div class="d-flex align-items-center gap-2 text-muted">
                    <img src="{{ asset('brand/itastock-logo.svg') }}" alt="ItaStock" width="28" height="28">
                    <span class="fw-semibold">ItaStock</span>
                    <span>·</span>
                    <span class="small">Gestión de stock para tu comercio</span>
                </div>
                <div class="d-flex gap-3">
                    <a class="text-decoration-none text-muted" href="{{ path('public_terms') }}">Términos</a>
                    <a class="text-decoration-none text-muted" href="{{ path('public_privacy') }}">Privacidad</a>
                </div>
            </div>
        </footer>

        {% if whatsappLink %}
            <a class="whatsapp-float" href="{{ whatsappLink }}" target="_blank" rel="noopener noreferrer" aria-label="Contactar por WhatsApp">
                <svg xmlns="http://www.w3.org/2000/svg" width="26" height="26" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                    <path d="M12.04 2.002c-5.523 0-10.02 4.477-10.02 9.999 0 1.766.466 3.493 1.353 5.015L2 22l5.17-1.354a9.963 9.963 0 0 0 4.87 1.264h.004c5.523 0 10.02-4.477 10.02-9.999 0-5.522-4.497-9.909-9.994-9.909zm.004 18.308h-.003a8.313 8.313 0 0 1-4.23-1.158l-.303-.18-3.064.803.815-2.99-.198-.308a8.31 8.31 0 0 1-1.276-4.39c0-4.586 3.74-8.316 8.35-8.316 4.598 0 8.34 3.73 8.34 8.316 0 4.586-3.742 8.223-8.431 8.223zm4.58-6.207c-.251-.125-1.49-.736-1.72-.82-.23-.084-.398-.126-.566.126-.168.25-.651.819-.797.987-.147.168-.293.188-.544.063-.251-.126-1.06-.39-2.02-1.245-.747-.666-1.252-1.49-1.4-1.742-.147-.25-.016-.385.11-.51.113-.112.251-.293.377-.44.126-.146.168-.25.251-.417.084-.168.042-.313-.021-.438-.063-.126-.566-1.365-.776-1.87-.204-.49-.412-.424-.566-.432l-.481-.01c-.168 0-.44.063-.67.313-.23.251-.88.86-.88 2.099 0 1.24.901 2.437 1.027 2.605.126.168 1.774 2.712 4.3 3.806.601.259 1.069.414 1.435.53.603.191 1.152.164 1.586.1.484-.073 1.49-.607 1.7-1.194.21-.586.21-1.09.147-1.194-.063-.105-.23-.168-.481-.293z"/>
                </svg>
            </a>
        {% endif %}
    </body>
</html>
