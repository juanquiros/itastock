<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; color: #222; }
        h1 { font-size: 18px; margin-bottom: 4px; }
        h2 { font-size: 14px; margin: 12px 0 6px; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 12px; }
        th, td { border: 1px solid #ddd; padding: 6px; }
        th { background: #f5f5f5; text-align: left; }
        .meta { margin-bottom: 12px; }
        .meta div { margin-bottom: 4px; }
    </style>
</head>
<body>
    <h1>Cierre de caja</h1>
    <div class="meta">
        <div><strong>Negocio:</strong> {{ business.name }}</div>
        <div><strong>Generado:</strong> {{ generatedAt|date('d/m/Y H:i') }}</div>
        <div><strong>Caja:</strong> #{{ cashSession.id }} · Apertura {{ cashSession.openedAt|date('d/m/Y H:i') }} · Cierre {{ cashSession.closedAt ? cashSession.closedAt|date('d/m/Y H:i') : '—' }}</div>
        <div><strong>Operador:</strong> {{ cashSession.openedBy.fullName }}</div>
    </div>

    <table>
        <tr><th>Inicial</th><td>${{ cashSession.initialCash }}</td></tr>
        <tr><th>Final contado</th><td>{{ summary.finalCash is not null ? '$' ~ summary.finalCash : '—' }}</td></tr>
        <tr><th>Efectivo esperado</th><td>${{ summary.cashExpected }}</td></tr>
        <tr><th>Diferencia</th><td>{{ summary.difference is not null ? '$' ~ summary.difference : '—' }}</td></tr>
    </table>

    <h2>Totales por medio de pago</h2>
    <table>
        <thead>
            <tr><th>Método</th><th class="text-end">Total</th></tr>
        </thead>
        <tbody>
            {% for method, total in summary.totals %}
                <tr><td>{{ method }}</td><td>${{ total }}</td></tr>
            {% else %}
                <tr><td colspan="2">Sin movimientos.</td></tr>
            {% endfor %}
        </tbody>
    </table>

    <h2>Ventas del período</h2>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Hora</th>
                <th>Método</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for sale in summary.sales %}
                <tr>
                    <td>{{ sale.saleId }}</td>
                    <td>{{ sale.createdAt ? sale.createdAt|date('d/m/Y H:i') : '' }}</td>
                    <td>{{ sale.paymentMethod }}</td>
                    <td>${{ sale.total }}</td>
                </tr>
                {% if includeDetail and (summary.saleDetails[sale.saleId] ?? []) is not empty %}
                    <tr>
                        <td colspan="4">
                            <table>
                                <thead>
                                    <tr>
                                        <th>Producto</th>
                                        <th style="text-align: right;">Cantidad</th>
                                        <th style="text-align: right;">Precio</th>
                                        <th style="text-align: right;">Total</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for item in summary.saleDetails[sale.saleId] %}
                                        <tr>
                                            <td>{{ item.description }}</td>
                                            <td style="text-align: right;">{{ item.qty }}</td>
                                            <td style="text-align: right;">${{ item.unitPrice }}</td>
                                            <td style="text-align: right;">${{ item.lineTotal }}</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </td>
                    </tr>
                {% endif %}
            {% else %}
                <tr><td colspan="4">Sin ventas en el rango.</td></tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
