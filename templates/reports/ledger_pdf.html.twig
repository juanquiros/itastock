<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; color: #222; }
        h1 { font-size: 18px; margin-bottom: 4px; }
        table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        th, td { border: 1px solid #ddd; padding: 6px; }
        th { background: #f5f5f5; text-align: left; }
        .meta { margin-bottom: 12px; }
        .totals { margin-top: 12px; text-align: right; }
    </style>
</head>
<body>
    <h1>Libro maestro</h1>
    <div class="meta">
        <div><strong>Comercio:</strong> {{ business.name }}</div>
        <div><strong>Periodo:</strong> {{ from|date('d/m/Y') }} - {{ to|date('d/m/Y') }}</div>
        <div><strong>Generado:</strong> {{ generatedAt|date('d/m/Y H:i') }}</div>
    </div>

    <table>
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Tipo</th>
                <th>Referencia</th>
                <th>Debe</th>
                <th>Haber</th>
                <th style="text-align: right;">Importe</th>
                <th>Nota</th>
            </tr>
        </thead>
        <tbody>
            {% for row in report.rows %}
                <tr>
                    <td>{{ row.date|date('d/m/Y') }}</td>
                    <td>{{ row.type }}</td>
                    <td>{{ row.reference }}</td>
                    <td>{{ row.debitAccount }}</td>
                    <td>{{ row.creditAccount }}</td>
                    <td style="text-align: right;">{{ row.amount|number_format(2, '.', ',') }}</td>
                    <td>{{ row.note }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7">No hay movimientos en el período.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="totals">
        <div><strong>Total débitos:</strong> {{ report.summary.totalDebits|number_format(2, '.', ',') }}</div>
        <div><strong>Total créditos:</strong> {{ report.summary.totalCredits|number_format(2, '.', ',') }}</div>
    </div>
</body>
</html>
