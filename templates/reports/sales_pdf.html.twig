<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; color: #222; }
        h1 { font-size: 18px; margin-bottom: 4px; }
        table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        th, td { border: 1px solid #ddd; padding: 6px; }
        th { background: #f5f5f5; }
        .meta { margin-bottom: 8px; }
    </style>
</head>
<body>
    <h1>Reporte de ventas</h1>
    <div class="meta">
        <div><strong>Negocio:</strong> {{ business.name }}</div>
        <div><strong>Generado:</strong> {{ generatedAt|date('d/m/Y H:i') }}</div>
        <div><strong>Rango:</strong> {{ from|date('d/m/Y') }} - {{ to|date('d/m/Y') }}</div>
    </div>

    <table>
        <thead>
            <tr>
                <th>Fecha/Hora</th>
                <th>ID</th>
                <th>Vendedor</th>
                <th>Cliente</th>
                <th>Método</th>
                <th>Estado</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for row in rows %}
                <tr>
                    <td>{{ row.createdAt ? row.createdAt|date('d/m/Y H:i') : '' }}</td>
                    <td>{{ row.saleId }}</td>
                    <td>{{ row.sellerEmail ?? 'N/D' }}</td>
                    <td>{{ row.customerName }}</td>
                    <td>{{ row.paymentMethod }}</td>
                    <td>{{ row.saleStatus == 'VOIDED' ? 'ANULADA' : 'CONFIRMADA' }}</td>
                    <td>${{ row.total }}</td>
                </tr>
            {% else %}
                <tr><td colspan="7">No hay ventas en el rango.</td></tr>
            {% endfor %}
        </tbody>
    </table>

    <p><strong>Total ventas:</strong> ${{ total }}</p>
</body>
</html>
