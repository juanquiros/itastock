<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; color: #222; }
        h1 { font-size: 18px; margin-bottom: 4px; }
        table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        th, td { border: 1px solid #ddd; padding: 6px; }
        th { background: #f5f5f5; }
    </style>
</head>
<body>
    <h1>Reporte de deudores</h1>
    <div><strong>Negocio:</strong> {{ business.name }}</div>
    <div><strong>Generado:</strong> {{ generatedAt|date('d/m/Y H:i') }}</div>
    <div><strong>Saldo mínimo:</strong> ${{ minBalance|number_format(2, '.', ',') }}</div>

    <table>
        <thead>
            <tr>
                <th>Cliente</th>
                <th>Teléfono</th>
                <th>Saldo</th>
                <th>Última actividad</th>
            </tr>
        </thead>
        <tbody>
            {% for row in items %}
                <tr>
                    <td>{{ row.customer.name }}</td>
                    <td>{{ row.customer.phone ?? 'N/D' }}</td>
                    <td>${{ row.balance|number_format(2, '.', ',') }}</td>
                    <td>{{ row.lastMovement ? row.lastMovement|date('d/m/Y H:i') : 'N/D' }}</td>
                </tr>
            {% else %}
                <tr><td colspan="4">Sin deudores para el filtro.</td></tr>
            {% endfor %}
        </tbody>
    </table>

    <p><strong>Total adeudado:</strong> ${{ totalDebt }}</p>
</body>
</html>
