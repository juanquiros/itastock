{% extends 'base.html.twig' %}

{% block title %}Nueva categoría · ItaStock{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <div>
            <p class="text-uppercase text-muted mb-1 small fw-semibold">Productos</p>
            <h1 class="h4 mb-0">Nueva categoría</h1>
            <p class="text-secondary mb-0">Agrupá tus productos y hacé más fácil buscarlos.</p>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            {{ include('category/_form.html.twig', { form: form }) }}
        </div>
    </div>
{% endblock %}
