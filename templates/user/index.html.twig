{% extends 'base.html.twig' %}

{% block title %}Usuarios | ItaStock{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h1 class="h4 mb-1">Usuarios</h1>
            <p class="text-muted mb-0">Gestiona administradores y vendedores.</p>
        </div>
        <a class="btn btn-primary" href="{{ path('app_user_new') }}">Nuevo usuario</a>
    </div>

    <div class="card shadow-sm">
        <div class="table-responsive">
            <table class="table mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Nombre</th>
                        <th>Correo</th>
                        <th>Rol</th>
                        <th>Puesto</th>
                        <th>Comercio</th>
                        <th class="text-end">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                {% for membership in memberships %}
                    <tr>
                        <td>{{ membership.user.fullName }}</td>
                        <td>{{ membership.user.email }}</td>
                        <td>
                            {% if membership.role == 'OWNER' %}
                                <span class="badge bg-primary">Dueño</span>
                            {% elseif membership.role == 'ADMIN' %}
                                <span class="badge bg-primary">Admin</span>
                            {% elseif membership.role == 'SELLER' %}
                                <span class="badge bg-success">Vendedor</span>
                            {% else %}
                                <span class="badge bg-secondary">Solo lectura</span>
                            {% endif %}
                        </td>
                        <td>{{ membership.user.posNumber ?: (membership.role in ['OWNER', 'ADMIN'] ? 1 : '-') }}</td>
                        <td>{{ membership.business.name }}</td>
                        <td class="text-end">
                            <div class="d-flex justify-content-end gap-2">
                                <a class="btn btn-sm btn-outline-primary" href="{{ path('app_user_edit', {id: membership.user.id}) }}">Editar</a>
                                <form method="post" action="{{ path('app_user_delete', {id: membership.user.id}) }}" onsubmit="return confirm('¿Eliminar este usuario?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membership.user.id) }}">
                                    <button class="btn btn-sm btn-outline-danger" type="submit">Eliminar</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-center text-muted py-4">No hay usuarios registrados.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
