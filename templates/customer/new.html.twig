{% extends 'base.html.twig' %}

{% block title %}Nuevo cliente - ItaStock{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <div>
            <p class="text-uppercase text-muted mb-1 small">Clientes</p>
            <h1 class="h3 mb-0">Nuevo cliente</h1>
            <p class="mb-0 text-muted">Cargá los datos para identificarlo en ventas.</p>
        </div>
    </div>

    <div class="card shadow-sm p-4">
        {{ include('customer/_form.html.twig', { form: form }) }}
    </div>
{% endblock %}
