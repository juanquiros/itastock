{% extends 'base.html.twig' %}

{% block title %}Editar cliente - ItaStock{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <div>
            <p class="text-uppercase text-muted mb-1 small">Clientes</p>
            <h1 class="h3 mb-0">Editar cliente</h1>
            <p class="mb-0 text-muted">Actualizá los datos del cliente seleccionado.</p>
        </div>
    </div>

    <div class="card shadow-sm p-4">
        {{ include('customer/_form.html.twig', { form: form }) }}
    </div>
{% endblock %}
