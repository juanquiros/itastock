<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; color: #222; }
        h1 { font-size: 18px; margin-bottom: 4px; }
        h2 { font-size: 14px; margin: 10px 0 6px; }
        table { width: 100%; border-collapse: collapse; margin-top: 8px; }
        th, td { border: 1px solid #ddd; padding: 6px; }
        th { background: #f5f5f5; }
        .movement-detail { margin: 6px 0 12px; padding: 8px 10px; background: #fafafa; border: 1px solid #e2e2e2; border-radius: 6px; }
        .movement-detail h3 { font-size: 12px; margin: 0 0 6px; }
        .detail-list { margin: 0; padding-left: 18px; }
        .detail-list li { margin-bottom: 4px; }
        .detail-meta { color: #555; font-size: 11px; }
    </style>
</head>
<body>
    <h1>Estado de cuenta</h1>
    <div><strong>Negocio:</strong> {{ business.name }}</div>
    <div><strong>Cliente:</strong> {{ customer.name }}</div>
    <div><strong>Generado:</strong> {{ generatedAt|date('d/m/Y H:i') }}</div>
    <div><strong>Rango:</strong> {{ from ? from|date('d/m/Y') : 'N/D' }} - {{ to ? to|date('d/m/Y') : 'N/D' }}</div>

    <h2>Resumen</h2>
    <table>
        <tr><th>Saldo actual</th><td>${{ data.balance }}</td></tr>
        <tr><th>Débitos período</th><td>${{ data.totalDebit }}</td></tr>
        <tr><th>Créditos período</th><td>${{ data.totalCredit }}</td></tr>
    </table>

    <h2>Movimientos {% if detailed %}(detallado){% endif %}</h2>
    <table>
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Tipo</th>
                <th>Importe</th>
                <th>Referencia</th>
                <th>Usuario</th>
                <th>Nota</th>
            </tr>
        </thead>
        <tbody>
            {% for movement in data.movements %}
                <tr>
                    <td>{{ movement.createdAt|date('d/m/Y H:i') }}</td>
                    <td>{{ movement.type }}</td>
                    <td>${{ movement.amount }}</td>
                    <td>{{ movement.referenceType }} {{ movement.referenceId ?? '' }}</td>
                    <td>{{ movement.createdBy ? movement.createdBy.fullName : 'N/D' }}</td>
                    <td>{{ movement.note ?? '' }}</td>
                </tr>
                {% if detailed and movement.referenceId and data.saleDetails[movement.referenceId] is defined %}
                    <tr>
                        <td colspan="6">
                            <div class="movement-detail">
                                <h3>Detalle del ticket #{{ movement.referenceId }}</h3>
                                <ul class="detail-list">
                                    {% for item in data.saleDetails[movement.referenceId] %}
                                        <li>
                                            {{ item.description }}
                                            <span class="detail-meta">· Cantidad: {{ item.qty }} · Precio: ${{ item.unitPrice }} · Total: ${{ item.lineTotal }}</span>
                                        </li>
                                    {% else %}
                                        <li>Sin detalle disponible.</li>
                                    {% endfor %}
                                </ul>
                            </div>
                        </td>
                    </tr>
                {% endif %}
            {% else %}
                <tr><td colspan="5">Sin movimientos en el período.</td></tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
