<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <title>Etiquetas de productos</title>
        <style>
            @page {
                size: A4;
                margin: 10mm;
            }

            body {
                font-family: 'Inter', sans-serif;
                font-size: 10pt;
                color: #111827;
                margin: 0;
                padding: 0;
            }

            .label-grid {
                display: grid;
                grid-template-columns: repeat(5, 35mm);
                grid-auto-rows: 60mm;
                gap: 3mm;
                justify-content: center;
            }

            .label {
                border: 1px solid #e5e7eb;
                border-radius: 3px;
                padding: 3mm 2.5mm;
                box-sizing: border-box;
                width: 35mm;
                height: 60mm;
                display: flex;
                flex-direction: column;
                justify-content: flex-start;
                page-break-inside: avoid;
            }

            .label-name {
                font-weight: 600;
                font-size: 9pt;
                line-height: 1.1;
                min-height: 8mm;
                margin-bottom: 1.5mm;
                text-align: center;
            }

            .label-meta {
                font-size: 8pt;
                color: #374151;
            }

            .label-price {
                font-weight: 700;
                font-size: 10pt;
                margin-top: 0.5mm;
            }

            .label-body {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 2mm;
                align-items: center;
            }

            .label-body--full {
                grid-template-columns: 1fr;
            }

            .label-image {
                width: 100%;
                height: 30mm;
                border-radius: 2px;
                border: 1px solid #e5e7eb;
                overflow: hidden;
                display: flex;
                align-items: center;
                justify-content: center;
                background: #f9fafb;
            }

            .label-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

            .barcode {
                text-align: center;
            }

            .barcode img {
                max-width: 100%;
                height: 14mm;
                object-fit: contain;
            }

            .barcode-value {
                font-size: 7.5pt;
                margin-top: 1mm;
                color: #374151;
                word-break: break-all;
            }
        </style>
    </head>
    <body>
        <div class="label-grid">
            {% for label in labels %}
                <div class="label">
                    <div class="label-name">{{ label.product.name }}</div>
                    {% if not options.showOnlyName %}
                        <div class="label-meta">SKU: {{ label.product.sku }}</div>
                        {% if options.showPrice %}
                            <div class="label-price">${{ label.product.basePrice|number_format(2, '.', ',') }}</div>
                        {% endif %}
                    {% endif %}
                    <div class="label-body {{ options.includeLabelImage and labelImagePath ? '' : 'label-body--full' }}">
                        {% if options.includeLabelImage and labelImagePath %}
                            <div class="label-image">
                                <img src="{{ labelImagePath }}" alt="Imagen de etiqueta">
                            </div>
                        {% endif %}
                        {% if options.includeBarcode %}
                            <div class="barcode">
                                {% if label.barcodeDataUri %}
                                    <img src="{{ label.barcodeDataUri }}" alt="Código de barras">
                                    <div class="barcode-value">{{ label.barcodeValue }}</div>
                                {% else %}
                                    <div class="label-meta">Sin código de barras</div>
                                {% endif %}
                            </div>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
    </body>
</html>
