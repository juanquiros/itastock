<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <title>Etiquetas de productos</title>
        {% set useLabelImage = options.includeLabelImage and labelImagePath %}
        {% set pageWidth = 210 %}
        {% set pageHeight = 297 %}
        {% set pageMargin = 10 %}
        {% if useLabelImage %}
            {% set labelWidth = 40 %}
            {% set labelHeight = 60 %}
            {% set columns = 4 %}
            {% set gap = 4 %}
        {% else %}
            {% set labelWidth = 50 %}
            {% set labelHeight = 20 %}
            {% set columns = 3 %}
            {% set gap = 4 %}
        {% endif %}
        {% set contentWidth = pageWidth - (pageMargin * 2) %}
        {% set contentHeight = pageHeight - (pageMargin * 2) %}
        {% set gridWidth = (columns * labelWidth) + ((columns - 1) * gap) %}
        {% set rows = ((contentHeight + gap) // (labelHeight + gap)) %}
        {% set gridHeight = (rows * labelHeight) + ((rows - 1) * gap) %}
        {% set offsetLeft = ((contentWidth - gridWidth) / 2) %}
        {% set offsetTop = ((contentHeight - gridHeight) / 2) %}
        <style>
            @page {
                size: A4;
                margin: 10mm;
            }

            body {
                font-family: 'Inter', sans-serif;
                font-size: 10pt;
                color: #111827;
                margin: 0;
                padding: 0;
            }

            .label-grid {
                display: grid;
                grid-template-columns: repeat({{ columns }}, {{ labelWidth }}mm);
                grid-auto-rows: {{ labelHeight }}mm;
                gap: {{ gap }}mm;
                margin-left: {{ offsetLeft }}mm;
                margin-top: {{ offsetTop }}mm;
            }

            .label {
                border: 1px solid #e5e7eb;
                border-radius: 3px;
                box-sizing: border-box;
                display: flex;
                flex-direction: column;
                page-break-inside: avoid;
            }

            .label--with-image {
                padding: 3mm 2.5mm;
                width: {{ labelWidth }}mm;
                height: {{ labelHeight }}mm;
                justify-content: flex-start;
            }

            .label--compact {
                padding: 2mm 2.5mm;
                width: {{ labelWidth }}mm;
                height: {{ labelHeight }}mm;
                flex-direction: row;
                align-items: center;
                justify-content: space-between;
                gap: 2mm;
                overflow: hidden;
            }

            .label-name {
                font-weight: 600;
                font-size: 8pt;
                line-height: 1.1;
                min-height: 7mm;
                margin-bottom: 1mm;
                text-align: center;
            }

            .label-name--compact {
                font-size: 7.5pt;
                line-height: 1.1;
                min-height: auto;
                margin: 0;
                text-align: left;
            }

            .label-meta {
                font-size: 7pt;
                color: #374151;
            }

            .label-price {
                font-weight: 700;
                font-size: 9pt;
                margin-top: 0.5mm;
            }

            .label-info {
                display: flex;
                flex-direction: column;
                gap: 0.5mm;
            }

            .label-body {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 2mm;
                align-items: center;
                margin-top: 1mm;
            }

            .label-body--full {
                grid-template-columns: 1fr;
            }

            .label-image {
                width: 100%;
                height: 26mm;
                border-radius: 2px;
                border: 1px solid #e5e7eb;
                overflow: hidden;
                display: flex;
                align-items: center;
                justify-content: center;
                background: #f9fafb;
            }

            .label-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

            .barcode {
                text-align: center;
                overflow: hidden;
            }

            .barcode img {
                max-width: 100%;
                height: 9mm;
                object-fit: contain;
            }

            .barcode-vertical {
                display: flex;
                align-items: center;
                justify-content: center;
                height: 26mm;
                overflow: hidden;
            }

            .barcode-vertical img {
                height: 20mm;
                width: auto;
                transform: rotate(90deg);
            }

            .barcode-value {
                font-size: 7pt;
                margin-top: 0.5mm;
                color: #374151;
                word-break: break-all;
            }

            .barcode-value--compact {
                font-size: 6.5pt;
                margin-top: 0.2mm;
                text-align: right;
            }
        </style>
    </head>
    <body>
        <div class="label-grid">
            {% for label in labels %}
                <div class="label {{ useLabelImage ? 'label--with-image' : 'label--compact' }}">
                    {% if useLabelImage %}
                        <div class="label-name">{{ label.product.name }}</div>
                        {% if not options.showOnlyName %}
                            <div class="label-meta">SKU: {{ label.product.sku }}</div>
                            {% if options.showPrice %}
                                <div class="label-price">${{ label.product.basePrice|number_format(2, '.', ',') }}</div>
                            {% endif %}
                        {% endif %}
                        <div class="label-body">
                            <div class="label-image">
                                <img src="{{ labelImagePath }}" alt="Imagen de etiqueta">
                            </div>
                            {% if options.includeBarcode %}
                                <div class="barcode">
                                    {% if label.barcodeDataUri %}
                                        <div class="barcode-vertical">
                                            <img src="{{ label.barcodeDataUri }}" alt="Código de barras">
                                        </div>
                                        <div class="barcode-value">{{ label.barcodeValue }}</div>
                                    {% else %}
                                        <div class="label-meta">Sin código de barras</div>
                                    {% endif %}
                                </div>
                            {% endif %}
                        </div>
                    {% else %}
                        <div class="label-info">
                            <div class="label-name label-name--compact">{{ label.product.name }}</div>
                            {% if not options.showOnlyName %}
                                <div class="label-meta">SKU: {{ label.product.sku }}</div>
                                {% if options.showPrice %}
                                    <div class="label-price">${{ label.product.basePrice|number_format(2, '.', ',') }}</div>
                                {% endif %}
                            {% endif %}
                        </div>
                        {% if options.includeBarcode %}
                            <div class="barcode">
                                {% if label.barcodeDataUri %}
                                    <img src="{{ label.barcodeDataUri }}" alt="Código de barras">
                                    <div class="barcode-value barcode-value--compact">{{ label.barcodeValue }}</div>
                                {% else %}
                                    <div class="label-meta">Sin código de barras</div>
                                {% endif %}
                            </div>
                        {% endif %}
                    {% endif %}
                </div>
            {% endfor %}
        </div>
    </body>
</html>
